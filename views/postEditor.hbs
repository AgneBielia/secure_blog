<!DOCTYPE html>
<html lang="en">
<h5 class="text-center pt-2">{{title}}</h5>
<div class="container">
    <div class="card m-auto w-75 border-0 bg-dark">
        <div class="form-floating w-100 m-auto">
            <form action="{{#if post.id}}/posts/{{post.id}}/edit{{else}}/posts/new{{/if}}" method="POST">
                <div class="mb-3">
                    <label for="title" class="form-label">Title:</label>
                    <input maxlength="100" class="form-control border-dark bg-light text-bg-dark bg-opacity-10" type="text" id="title" name="title" value="{{post.title}}">
                </div>
                <div class="mb-3">
                    <label class="form-label" for="content">Content:</label>
                    <textarea style="height: 300px" class="rounded-3 w-100 border-dark bg-light text-bg-dark bg-opacity-10" id="content" name="content">{{post.content}}</textarea>
                </div>
                {{#if error}}
                    <div class="alert alert-dark alert-dismissible fade show" role="alert">
                        {{error}}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {{/if}}
                <button class="btn btn-outline-success" type="submit">Save</button>
                <a role="button" class="btn btn-outline-light" href="/posts/{{post.id}}">Cancel</a>
                <input type="hidden" name="_csrf" value="{{csrfToken}}">
            </form>
        </div>
    </div>
</div>
</html>